define([
    'd3',
    'json!data/sampleData.json'
], function(
    d3,
    sampleData
) {
    'use strict';
    function render(data) {
        //console.log(sampleData);
        //console.log(data);
    }

    return {
        render: render
    };
});
